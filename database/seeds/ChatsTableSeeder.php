<?php

use Illuminate\Database\Seeder;

class ChatsTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // $chat = factory(App\ChatRoom::class)->create([
        //     'user_id' => 3,
        //     'application_id' => 1,
        // ]);

        // $chat->messages()->saveMany(factory(App\Message::class, 10)->make([
        //     'sender_id' => rand(1, 2)
        // ]));
    }
}
